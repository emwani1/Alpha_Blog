<h1>Articles</h1>
<table>
  <p><%= link_to 'Create new article',new_article_path %></p>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Actions</th>
  </tr>
  <% @articles.each do |article| %>
  <tr>
    <td><%= article.title %></td>
    <td><%= article.description %></td>
    <td><%= link_to 'Edit',edit_article_path(article.id) %></td>
    <td><%= link_to 'Show Article',article_path(article.id) %></td>
  </tr>
    <% end %>
</table>
